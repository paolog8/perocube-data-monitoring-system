apiVersion: 1

providers:
  - name: repo
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/repo-dashboards
      foldersFromFilesStructure: true
